import React from 'react';
import CreateWorkout from './CreateWorkout';

const Workout = () => {
    return (
        <div>
            <CreateWorkout />
        </div>
    );
};

export default Workout;
