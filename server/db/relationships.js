const User = require('./models/User');
const Workout = require('./models/Workout');

//set up relationships here and just require the file
//it is best to probably set it up this way in order to prevent any circular dependencies
